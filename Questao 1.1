#include <stdio.h>

int strlen(char s[]) {
    int length = 0;

    while (s[length] != '\0') {
        length++;
    }

    return length;
}

int main() {
    char string[] = "Hello, world!";
    int length = strlen(string);
    printf("O comprimento da string é: %d\n", length);
    return 0;
}
